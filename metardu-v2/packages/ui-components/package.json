{
  "name": "@metardu/ui-components",
  "version": "0.1.0",
  "description": "React UI components for MetaRDU Desktop v2.0 — surveyor-facing panels for flight planning, stakeout, GNSS, drone control, LULC, cross-sections, and as-built comparison",
  "license": "MIT",
  "author": "error302",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "peerDependencies": {
    "react": "^18.0.0",
    "react-dom": "^18.0.0"
  },
  "devDependencies": {
    "@types/react": "^18.0.0",
    "@types/react-dom": "^18.0.0",
    "typescript": "^5.5.0"
  }
}
